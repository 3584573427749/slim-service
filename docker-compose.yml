version: "3.9"

services:
  slim-service:
    container_name: slim-service
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/var/www
    environment:
      DB_HOST: host.docker.internal
      DB_NAME: slim_db
      DB_USER: root
      DB_PASS: root
